## Mode: Needs Human Direction

The selector found a state that should not be resolved autonomously.

## Goal

Record the smallest decision Stuart must make before work can continue.

## Work Loop

1. Fetch the selected parent and relevant issue graph from Linear.
2. Record the unresolved question in Linear.
3. State the Codex position, Claude position if known, and the consequence of each option.
4. State whether any safe work remains while waiting.
5. Update `{{NANCY_CURRENT_TASK_DIR}}/HANDOVER.md` with the same coordination summary.

## Boundaries

- Do not implement source changes.
- Do not create speculative execution issues.
- Do not mark the task complete.
